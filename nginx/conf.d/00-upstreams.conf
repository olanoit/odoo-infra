# =============================================================================
# EXTENDRIX — Nginx Upstreams
# Los upstreams se agregan automáticamente via sync-projects.sh
# Nomenclatura: up_{proyecto}_{entorno}_{tipo}
# =============================================================================

# __SYNC_UPSTREAMS_INSERT__

# =============================================================================
# PLANTILLA — Copiar para nuevo proyecto (o usar sync-projects.sh)
# Reemplazar: {PROYECTO}  {ENTORNO}  {CONTENEDOR}
# =============================================================================
# upstream up_{PROYECTO}_{ENTORNO}_http {
#     server {CONTENEDOR}:8069 weight=1 max_fails=3 fail_timeout=30s;
#     keepalive 16;
# }
# upstream up_{PROYECTO}_{ENTORNO}_lp {
#     server {CONTENEDOR}:8072 weight=1 max_fails=3 fail_timeout=30s;
#     keepalive 8;
# }
